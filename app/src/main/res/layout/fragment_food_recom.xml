<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:context=".ui.menu.calorie.BigCaloriesFragment">

    <TextView
        android:id="@+id/header_bigCalories"
        android:text="@string/food_recommendation"
        style="@style/CustomTextView"
        android:textSize="24sp"
        android:textStyle="bold"
        android:layout_marginStart="21dp"
        android:layout_marginTop="21dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"/>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvActivity"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="15dp"
        android:scrollbars="horizontal"
        android:orientation="horizontal"
        tools:listitem="@layout/item_calorie"
        app:layout_constraintTop_toBottomOf="@id/header_bigCalories"/>

</androidx.constraintlayout.widget.ConstraintLayout>